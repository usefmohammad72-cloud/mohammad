# Local-only example. Never commit a file containing real credentials.

# Claude Code through OmniRoute
ANTHROPIC_BASE_URL=http://localhost:20128/v1
ANTHROPIC_AUTH_TOKEN=REPLACE_WITH_LOCAL_OMNIROUTE_KEY

# Optional OpenAI/Codex-compatible local gateway variables can be configured
# according to the installed Codex client. Keep real values outside Git.
